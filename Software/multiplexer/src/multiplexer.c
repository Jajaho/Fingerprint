#include <stdint.h>
#include "adg726.h"
#include "defines.h"


/// @brief  Selects a channel on a multiplexer
/// @param chip 0 for 
/// @param number 
/// @return 
int setChannel(uint8_t chip, uint8_t number) {
    if(chip == 'A') {
        SETBIT()
    }
}